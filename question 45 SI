class Interest:
    def __init__(self, p, r, t):
        self.p = p
        self.r = r
        self.t = t

    def calculate(self):
        return (self.p * self.r * self.t) / 100

p = float(input())
r = float(input())
t = float(input())

obj = Interest(p, r, t)
print(obj.calculate())